import { useState } from "react";

interface IGameMasterProps {}

export const GameMaster = (props: IGameMasterProps) => {
  //   const [item, setItem] = useState(props.item);

  return <div>I am a GameMaster component</div>;
};
